# Railway / Nixpacks build configuration
# 指定后端目录和启动命令

[phases.setup]
nixPkgs = ["...", "python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python main.py"

[build]
root = "gold-backend"
